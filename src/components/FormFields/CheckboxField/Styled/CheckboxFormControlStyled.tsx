import styled from 'styled-components'
import FormControl from '@mui/material/FormControl'

const CheckboxFormControlStyled = styled(FormControl)`
  height: 100%;
  justify-content: center;
`

export default CheckboxFormControlStyled
